#!/bin/bash

if [[ $1 = "random" ]]
then
    theme=$(ls "$HOME/.config/nitrogen/" -I *clean.cfg -I *saved.cfg -I nitrogen.cfg | shuf -n 1)
else
    theme="$HOME/.config/nitrogen/profile-$1.cfg"
fi

pushd "$HOME/.config/nitrogen"

cp $theme bg-saved.cfg
nitrogen --restore

popd
